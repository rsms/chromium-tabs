// ChromiumTabs

// Describes the |Browser| class which represents one set of tabs (a window).
#import <ChromiumTabs/Browser.h>

// Describes the |BrowserWindowController| class which represents a window with
// tabs. It also manages one |Browser| instance (owns the reference).
#import <ChromiumTabs/BrowserWindowController.h>

// Describes the |TabContents| class which represents the contents of a tab,
// including icon, title and state (loading, crashed, etc).
#import <ChromiumTabs/TabContents.h>
